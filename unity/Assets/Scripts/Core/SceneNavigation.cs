public static class SceneNavigation
{
    // Stores the previous scene to allow "Back" navigation from Test Mode scene.
    // Default falls back to "MainMenu" if not set.
    public static string PreviousScene = "MainMenu";
}
